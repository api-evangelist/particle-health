generated: '2026-08-14'
method: searched
source:
  - https://docs.particlehealth.com/docs/test-patient-sandbox
  - https://docs.particlehealth.com/docs/testing-signal-in-sandbox
  - https://docs.particlehealth.com/docs/more-sandbox-cases
  - https://api.github.com/repos/ParticleHealth/synthetic-patients-ccdas
base_url: https://sandbox.particlehealth.com
mode: separate host (not a header/key-prefix switch)
description: >-
  Particle Health runs a fully separate Sandbox host (https://sandbox.particlehealth.com), populated
  with synthetic patient demographics and mock medical records, supporting the exact same API surface
  (all products/workflows) as production. Sandbox data quality won an HHS-recognized award for
  synthetic-data realism per the docs page. Auth flow is identical to production (same GET /auth,
  client-id/client-secret/scope headers) — credentials are provisioned per-environment, so a
  production client_id/secret will not work against the sandbox host and vice versa.
limits:
  - scope: organization
    metric: queries_per_day
    limit: 500
    note: "\"we limit the number of queries within our Sandbox environment to 500 queries per organization
      per day\" — per docs.particlehealth.com/docs/test-patient-sandbox"
fixtures:
  synthetic_patients: >-
    Documented matrix of named synthetic patients, each mapped to which products (Signal/Workbench),
    data formats, and Snapshot templates they support — see the Test Patient Sandbox doc for the full
    table (not reproduced here verbatim due to length; table format preserved in the source page).
  synthetic_documents_repo:
    url: https://github.com/ParticleHealth/synthetic-patients-ccdas
    description: Public GitHub repo of synthetic patient C-CDA clinical documents, maintained by
      Particle Health for testing/demo use.
  sandbox_trigger_tooling:
    operationId: triggerSandboxWorkflow
    path: /api/v1/patients/{particle_patient_id}/subscriptions/trigger-sandbox-workflow
    source: openapi/particle-health-subscriptions-api-openapi.yml
    description: Sandbox-only endpoint that synthetically fires a Signal webhook notification for a
      given patient, for testing a webhook receiver end-to-end.
test_keys:
  note: >-
    No documented test-mode key PREFIX convention (e.g. no "sk_test_" style pattern found in docs or
    OpenAPI) — the sandbox/production split is entirely by host (sandbox.particlehealth.com vs.
    api.particlehealth.com), with separate credential pairs issued per environment.
time_simulation: not documented; no test-clock or time-travel mechanism found.
maintainers:
  - FN: Kin Lane
    email: kin@apievangelist.com
